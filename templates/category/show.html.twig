{% extends 'base-backend.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<a href="{{ path('app_category_index') }}"class="btn btn-dark">back to list</a>
<h1>Category</h1>
    <div class="d-flex justify-content-end align-items-end">
    {{ include('category/_delete_form.html.twig') }}
    <a href="{{ path('app_category_edit', {'id': category.id}) }}"class="btn btn-success ms-2"><i class="align-middle" data-feather="edit"></i></a>
</div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
