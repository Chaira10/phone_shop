{% extends 'base.html.twig' %}

{% block title %}Nos smartphones{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/css/smartphone.css')}}">
  
        {% endblock %}
            {% block carousel %}
            <div class="container-fluid bg-dark mb-3 p-5">
        {# <div class="jumbotron jumbotron-fluid"> #}
        <div class="row">

    <div class="col-md-12 bg-light">
        <img src="{{ asset('/images/products/accessoires.png') }}" class="img-fluid" alt="..." style="width:100%">
        </div>
</div>

         <div class="container-fluid">
           <h1 class="display-4 text-light">
             Une gamme majeure. Et magique. !
           </h1>
           <p class="lead font-italic text-secondary">
             "Une connexion magique à vos appareils."
           </p>
         </div>
       
                
       {# </div> #}
</div>
    {% endblock %}
{% block content %}

      
    <div class="container-fluid">
<div class="row">

    <div class="col-md-12">
    <h1>Nos accessoires</h1>

<div class="row product-container">
    
    {# {% for category in categorys %} #}
    {# {% if category.id == 4 %} #}
    {% for product in products %}
    {% if product.category.name == 'Accessoires' %}

    
    

    {# {{ dump(product)}} #}
        <div class="col-md-6">
                <div class="product-item text-center">
                    <a href="{{ path('app_smartphone_product', {'slug' : product.slug })}}"><img src="{{ asset('images/products/' ~ product.image) }}" alt="{{ product.name }}" class="img-fluid"></a>
                    <h5>{{ product.name }}</h5>
                    <span class="product-subtitle">{{ product.subtitle }}</span><br/>
                    <span class="product-price">{{ product.price }}€</span><br/>
                    <a href="{{ path('app_smartphone_product', {'slug' : product.slug })}}"><button class="btn btn-primary">Acheter</button></a>
                </div>
                
        </div>


    {% endif %}
    {% endfor %}
    {# {% endfor %} #}
</div>
    </div>
</div>
</div>
<div class="container-fluid">
<div class="row">

    <div class="col-md-12">
    <h1>Nos montres</h1>

<div class="row product-container">
    
    {# {% for category in categorys %} #}
    {# {% if category.id == 4 %} #}
    {% for product in products %}
    {% if product.category.name == 'Montre' %}

    
    

    {# {{ dump(product)}} #}
        <div class="col-md-6">
                <div class="product-item text-center">
                    <a href="{{ path('app_accessoires', {'slug' : product.slug })}}"><img src="{{ asset('images/products/' ~ product.image) }}" alt="{{ product.name }}" class="img-fluid"></a>
                    <h5>{{ product.name }}</h5>
                    <span class="product-subtitle">{{ product.subtitle }}</span><br/>
                    <span class="product-price">{{ product.price }}€</span><br/>
                    <a href="{{ path('app_accessoires', {'slug' : product.slug })}}"><button class="btn btn-primary">Acheter</button></a>
                </div>
                
        </div>


    {% endif %}
    {% endfor %}
    {# {% endfor %} #}
</div>
    </div>
</div>
</div>
<div class="container-fluid">
<div class="row">

    <div class="col-md-12">
    <h1>Nos écouteur</h1>

<div class="row product-container">
    
    {# {% for category in categorys %} #}
    {# {% if category.id == 4 %} #}
    {% for product in products %}
    {% if product.category.name == 'Ecouteur' %}

    
    

    {# {{ dump(product)}} #}
        <div class="col-md-6">
                <div class="product-item text-center">
                    <a href="{{ path('app_accessoires', {'slug' : product.slug })}}"><img src="{{ asset('images/products/' ~ product.image) }}" alt="{{ product.name }}" class="img-fluid"></a>
                    <h5>{{ product.name }}</h5>
                    <span class="product-subtitle">{{ product.subtitle }}</span><br/>
                    <span class="product-price">{{ product.price }}€</span><br/>
                    <a href="{{ path('app_accessoires', {'slug' : product.slug })}}"><button class="btn btn-primary">Acheter</button></a>
                </div>
                
        </div>


    {% endif %}
    {% endfor %}
    {# {% endfor %} #}
</div>
    </div>
</div>
</div>
    
{% endblock %}
{% block body %}
<div class="container-fluid bg-dark mb-0 p-5">
        {# <div class="jumbotron jumbotron-fluid"> #}
        <div class="row">

    <div class="col-md-12  mb-0">
        <img src="{{ asset('/images/products/banniere.png') }}" class="img-fluid" alt="..." style="width:100%">
        </div>
</div>

         <div class="container-fluid">


         </div>
       
                
       {# </div> #}
</div>
{% endblock %}
{% block javascripts %} 
    <script src="{{ asset('/js/smartphone.js')}}"></script>

{% endblock %}