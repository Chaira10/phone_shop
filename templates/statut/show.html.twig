{% extends 'base-backend.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <a href="{{ path('app_statut_index') }}" class="btn btn-dark">back to list</a>
    <h1>Statut</h1>
    <div class="d-flex justify-content-end align-items-end">
        {{ include('statut/_delete_form.html.twig') }}
            <a href="{{ path('app_statut_edit', {'id': statut.id}) }}" class="btn btn-success ms-2"><i class="align-middle" data-feather="edit"></i></a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statut.name }}</td>
            </tr>
        </tbody>
    </table>






{% endblock %}
